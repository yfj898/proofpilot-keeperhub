[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "proofpilot-keeperhub"
version = "0.1.0"
description = "Verifiable, simulation-first KeeperHub execution agent prototype"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

